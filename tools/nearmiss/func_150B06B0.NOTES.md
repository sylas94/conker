# func_150B06B0 (game_DDB60.c, 944 B) — near-miss, 2026-08-11

`tools/nearmiss/func_150B06B0.c` is the WHOLE TU with the function live (pragma removed).
Drop it over `conker/src/game_DDB60.c` to reproduce.

## Result

* isolated compare: **mine = 236 instructions, gold = 236** — the instruction MULTISET is
  identical (every opcode, every register, every immediate). asm-differ `-R` and no-`-R` both
  give **585** (identical, so no hidden rodata relocation problem).
* residual is **100% instruction ORDER**: 0 inserts, 0 deletes, 0 wrong registers,
  0 wrong immediates, 0 wrong stack offsets.

## The whole residual, exactly

Two hunks, both "which address gets materialised first":

```
        GOLD                       MINE
157     addiu s0,sp,68   (&sp3C.unk08)     addiu t4,sp,308  (&sp134)
158     addiu t4,sp,308                    lw    at,0(t4)
159     lw    at,0(t4)                     addiu s0,sp,68
```
Golden computes the struct-copy DESTINATION before the SOURCE; IDO computes the source first
and slots the destination into the load-delay slot. That costs one filler slot per lw/sw pair,
so golden fits six `li` constants into the copy (unk2C, unk2E, unk02, unk04, unk06, unk30)
and mine only fits five — `li t5,3` (unk30) is displaced from row 169 to row 191. Rows
157–191 all shift; they re-sync at 192.

```
212     addiu a0,sp,60                     move  a1,s0
213     move  a1,s0                        addiu a0,sp,60
```
Same flavour: golden evaluates the func_15153F18 arg list a0-then-a1, mine a1-then-a0.

## Spellings ruled out — ALL SEVEN gave byte-identical output (585, 34 rows)

1. `sp3C.unk08 = sp134;` with `func_15153F18(&sp3C, &sp3C.unk08, ...)`   (baseline)
2. `Vec *p = &sp3C.unk08; *p = sp134; ... func_15153F18(&sp3C, p, ...)`
3. same as 2 with `register Vec *p`
4. `sp3C.unk08 = *(Vec150B06B0 *)&sp134.unk00;`  (cast form)
5. splitting the local into `Angles150B06B0 sp3C` (8 B) + `Spray150B06B0 sp44` (0x44 B),
   i.e. exactly the two parameter structs func_15153F18 itself declares in game_17CAF0.c,
   with `func_15153F18(&sp3C, &sp44, ...)` — a whole-local address, not a field address
6. joining the copy onto one source line with the next statement
7. moving `sp3C.unk2C = 7; sp3C.unk2E = 3;` to immediately after the copy

Two spellings that made it WORSE (so the current form is confirmed correct, not arbitrary):

* whole tail block joined onto ONE source line → 51 differing rows (up from 34). The `-g3`
  source-line scheduling lever IS live in this function, and it says golden is
  one-statement-per-line, as written.
* `sp3C.unk02` before `sp3C.unk00` (the order matched sibling `func_151382E0` uses) →
  35 rows AND it moved two registers (`lb t0`→`lb t1`, `li t7,120`→`li t0,120`).
  Current order (unk00 then unk02) reproduces golden's registers exactly.

## Register map

Identical to golden throughout — s0=arg0 then &sp3C.unk08, s1=arg2&0xFF, s2=arg3,
and every t-register matches. There is nothing left to steer at source level.

## Verdict

Textbook decomp-permuter seed: identical instruction multiset, pure list-scheduler tie on
`addiu s0` vs `addiu t4` priority. No source handle found in seven independent spellings.
