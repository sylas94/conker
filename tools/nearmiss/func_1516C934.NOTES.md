# func_1516C934 — 804 B / 201 insns, game_197C20.c — parked at **asm-differ 1836** (cold start)

`tools/nearmiss/func_1516C934.c` is the WHOLE TU with this function live (pragma removed).
Drop it over `conker/src/game_197C20.c` to resume. The repo copy of the TU is UNTOUCHED
(pragma still in place) — verified: `git status --short -- conker/src/game_197C20.c` prints
nothing and the restored TU rebuilds to score **0**.

## WAVE 2026-08-13 — first attempt. 2101 (first draft) → **1836**.

The function is **byte-exact in structure**: same frame (0x120), same stack offsets, same
basic-block graph, same callee argument setup, same semantics. It is **3 instructions short**
of golden's 201 and the rest of the residual is **register naming only**.

## THE FRAME DECODE (this is the load-bearing evidence — it was right first try)

```
framesize                       0x120 = 288
outgoing args (11-arg callee)   0x00 .. 0x2B      func_150417AC args 4..10 at 0x10..0x28
                                0x2C .. 0x37      (12 B compiler pad/temp)
saved f20,f22,f24,f26,f28       0x38 .. 0x5F
saved s0..s7,fp,ra              0x60 .. 0x87
compiler temp area              0x88 .. 0x9B      (20 B, no local ever touches it)
LOCALS  (top-down, decl order)  0x9C .. 0x11F     = 132 B
      1st  Gfx *orig            0x11C  (4)        sw s1,0x11c(sp) / lw v0,0x11c(sp) on early return
      2nd  u8   buf[0x54]       0x0C8 .. 0x11B    addiu s4,sp,0xc8; sb at s4+len
      3rd..10th  EIGHT scalars  0x0A8 .. 0x0C7    never touched -> all register-resident
      11th s32 w                0x0A4             func_150428D4 out-param 1
      12th s32 h                0x0A0             func_150428D4 out-param 2
      13th s32 d                0x09C             func_150428D4 out-param 3
```
`0x11C - 0xC8 = 0x54` FORCES `buf[84]`, and `(0xC8 - 0xA8)/4 = 8` FORCES exactly eight
4-byte locals between `buf` and `w`. My eight are `flags, cond, i, len, first, dx, y, xoff`
— which is exactly the number the function needs, so **there are no dead frame-shaping
locals**. Building this list produced framesize 0x120 and every offset correct on the first
compile. Their relative ORDER is unobservable (measured: three different orders, all 1836).

## SIGNATURES — all derived from golden, none guessed

```c
Gfx *func_1516C934(Gfx *gfx, Struct1516C934 *arg1, u8 arg2, u8 arg3, u8 arg4, u8 arg5);
```
* `arg2/arg3` arrive in a2/a3, are homed by `sw ...,0x128/0x12c(sp)` and re-read `lbu 0x12b/0x12f`
  -> **u8**. `arg4/arg5` are stack params read `lbu 0x133/0x137(sp)` -> **u8**.
* `Gfx *` (not `s32`) is proven: the `s32 gfx` form with `gfx += 8` scores **2266**.

```c
void func_150428D4(u8 *str, s32 *w, s32 *h, s32 *d);              /* still a #pragma stub */
Gfx *func_150417AC(Gfx *, f32, f32, u8 *, u8, u8, u8, u8, f32, f32, s32);
```
* `func_150417AC` arity 11 confirmed by reading its own frame: framesize 0x1B8, highest
  incoming slot read is `lw $t8, 0x1E0($sp)` = arg10. args 4..7 are `lbu 0x1CB/0x1CF/0x1D3/0x1D7`
  -> u8; args 8,9 are `lwc1 0x1D8/0x1DC` -> f32.
* Neither callee is declared anywhere in the tree (both are `#pragma GLOBAL_ASM` in
  game_6E770.c), so both prototypes here are read off the golden asm, not inherited guesses.
* `D_800BE9C8[]`/`D_800BEBA4`/`D_800BE9C0` and the overflow idiom are copied VERBATIM from
  **live matched C** — game_58F80.c:329 (`func_1502C974`) and game_215960.c:99 (`func_151E8620`):
  `if (((g - D_800BE9C8[D_800BE9C0]) >> 3) > D_800BEBA4) { cond = 1; } else { cond = 0; }
   if (cond) { return orig; }` — this reproduces golden's materialise-then-test shape exactly.
* `WGFX1516C934` is the standard local macro of this family (game_6E770.c, game_1944C0.c).

## WHAT MOVED THE SCORE

| change | score |
|---|---|
| first draft | 2101 |
| **`if (buf[len] == 0) { draw } else { len++; }`** (golden's `bnez v1` puts the draw on the fallthrough) | **1836** |
| `Struct1516C934 *p = arg1;` used for the skip-loop condition (replacing the `flags` local) | 1811 |

The 1811 is **NOT parked**: it buys 25 points by deleting `flags` — a local for which there is
positive evidence (`lw t1,0x10(s2); lw v0,0(t1)` feeding three tests through `$v0`, a
high-ranked web) — and replacing it with a pointer alias used at exactly one site. That is an
unjustified frame-shaping construct, not a reading of the binary. Recorded, refused.

## MEASURED NEGATIVES (all on the 1836 base, all exactly 1836 = byte-identical unless noted)

| axis | n | result |
|---|---|---|
| skip loop `while` vs explicit `if (c) do {} while (c)` vs `for (;C;i++)` | 3 | 1836 |
| declaration order of the eight register-resident scalars (3 permutations) | 3 | 1836 |
| `i = len = 0;` / `len = i = 0;` / separate | 3 | 1836 |
| index spelling `arg1->f24[i]` vs `*(i + arg1->f24)` (head, tail, both) | 3 | 1836 |
| `first = 1;` before vs after the skip loop | 2 | 1836 |
| `flags` declared `u32`; `y += (f32)d` written `y = y + (f32)d` | 2 | 1836 |
| `i = 0` moved to just before the skip loop / into a `for` header | 2 | **2066** |
| main loop as `for (;;) { ... if (str[i++]==0) break; }` | 2 | **6213** |
| `s32 gfx` parameter + `gfx += 8` (the game_58F80 spelling) | 1 | **2266** |

## THE RESIDUAL — 3 missing instructions, then pure register naming

Mine is 198 insns, golden 201. All three missing instructions are the same phenomenon:
**golden refuses to share the `arg1->field_0x24` load across blocks, mine shares it.**

```
GOLD 221c  move t0,s2                 MINE  (absent)          <- a copy of arg1, made at the TOP
GOLD 227c  lw   t9,0x24(t0)           MINE 2278 move v0,t0    <- skip-loop preheader:
GOLD 2280  addu v0,t9,s3                                         golden re-loads and does NOT
                                                                 constant-fold i==0; mine folds
GOLD 2478  b 2488                     MINE 246c b 2478        <- loop bottom: golden fills the
GOLD 247c   lw v1,0x24(s2)  (delay)   MINE 2470  add.s(y+=d)     delay slot by DUPLICATING the
GOLD 2480  addiu s0,s0,1              MINE 2474 addiu s0,s0,1    merge block's load; mine fills
GOLD 2484  lw v1,0x24(s2)             MINE 2478 lw t0,0x24(s5)   it with the block's last insn
```

Everything else is one consistent register permutation, and it is a **rotation of exactly
three saved registers**:

```
golden:  s0=len  s1=gfx  s2=arg1  s3=i  s4=&buf  s5=arg5   f20=y     f22=4096.0f
mine:    s0=len  s1=gfx  s2=&buf  s3=i  s4=arg5  s5=arg1   f22=y     f20=4096.0f
```
s0/s1/s3/s6(first)/s7(const 10)/s8(arg4) already agree. Golden ranks `arg1` above `&buf` above
`arg5`; mine ranks `&buf` > `arg5` > `arg1`. **This is almost certainly downstream of the three
missing instructions** — they are all `arg1` references (`move t0,s2` plus two extra
`lw ...(s2)`), i.e. golden's `arg1` web has 10 references and mine has 8. Also note
`addu a0,s3,v1` (golden) vs `addu t7,t0,s3` (mine): golden puts the *variable* first, which is
NOT IDO's canonical temp-first form — reversing the source spelling does not reproduce it
(measured, 1836), so that too is a consequence of the different web, not of the spelling.

## NEXT MOVES, in the order I would try them

1. **Make the loop-bottom delay slot take the duplicated load.** One instruction, purely
   structural, and the cheapest of the three. IDO chose the block's last instruction
   (`y += (f32)d`) in mine; golden could have and did not. Find what makes `add.s` ineligible
   — e.g. an extra statement after `y += (f32)d;` in the then-branch, or `d` read at a
   different point.
2. **Defeat the constant-fold of `i == 0` in the skip-loop preheader.** Golden emits
   `addu v0,t9,s3` for the induction-pointer init; mine folds to `move v0,t0`. Everything
   tried on the loop *form* is inert, so the lever is probably on `i` itself.
3. Only after 1+2, re-read the saved-register ranking. Do not chase the s2/s4/s5 rotation
   directly — 12 measured variants say the source spelling does not move it.
